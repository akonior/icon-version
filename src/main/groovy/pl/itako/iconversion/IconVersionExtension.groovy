package pl.itako.iconversion

/**
 * Allow users of this extension to customize properties of their builds
 */
class IconVersionExtension {

    /**
     * Configuration for how the final overlaid information is drawn
     */
    RenderingConfig renderingConfig = RenderingConfig.DEFAULT
}